Usage:

1. Load the tests in Selenium and export them via Perl-Webdriver-Custom
2. Run fix-all.sh <dir_to_tests>
    where <dir_to_tests> is the directory where the tests reside
3. Run test.sh <dir_to_tests>
    where <dir_to_tests> is the directory where the tests reside
